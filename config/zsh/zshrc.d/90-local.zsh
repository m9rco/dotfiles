# 本机专属覆盖。必须最后加载，才能覆盖前面所有片段的设置。
#
# ~/.zshrc.local 不入库、不由安装流程创建 —— 它是机器专属的，
# 放不适合共享的东西：工作项目路径、私有别名、单机的环境变量。
#
# 需要密钥时不要写在这里明文（这个文件权限通常是 644）。
# 用密钥管理能力提供的按需注入函数，见 config/ 下的 secrets 相关配置。

[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"

# 目录级的环境变量交给 direnv（见 30-tools.zsh），不要在这里做 cd hook。

# 显式成功返回。片段的退出状态就是最后一条命令的状态 ——
# 没有 ~/.zshrc.local 时上面的 [[ ]] 返回假，会被入口误报为"片段失败"。
true
